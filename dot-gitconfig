[user]
  name = Lattapon Yodsuwan
  email = i@clozed2u.com
  username = clozed2u

[core]
  autocrlf = false
  safecrlf = true

[apply]
  whitespace = nowarn

[color]
  branch = auto
  diff = auto
  status = auto
  interactive = auto
  ui = always
  pager = true

[credential]
  helper = osxkeychain

[push]
  default = current

[pull]
  default = current

[alias]
  aa = add --all
  bl = blame -C
  br = branch
  ci = commit -qm
  co = checkout
  cl = clone
  fe = fetch
  in = init
  mg = merge
  ps = push
  pu = pull
  rm = rm -rf
  st = status -sb
  un = reset HEAD --
  df = diff --ignore-space-at-eol --ignore-blank-lines --color-words -b -w
  ll = log --pretty='format:%C(yellow)%h %C(blue)%ad %C(reset)%s%C(red)%d %C(green)%an%C(reset), %C(cyan)%ar' --date=short
  lll = log --color --graph --pretty=format:'%C(red)%h%C(reset) - %C(yellow)%d%C(reset) %s %C(green)%cr%C(reset) %C(bold blue)<%an - %ae>%C(reset)'
  search = log --no-merges -i -E --pretty='%h (%ad) - [%an] %s %d' --date=format:'%b %d %Y' —grep
  cb = "!git checkout `git branch | fzy`"

[url "https://github.com/"]
  insteadOf = gh:

[url "https://bitbucket.org/"]
  insteadOf = bb:
